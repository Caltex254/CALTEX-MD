#!/usr/bin/env sh
. "$(dirname -- "$0")/_/husky.sh"

# =============================================================================
# CALTEX MD - Pre-commit Hook
# =============================================================================

echo "🔍 Running pre-commit checks..."

# Run lint-staged
npx lint-staged

# Type check
echo "🔎 Running type check..."
npx tsc --noEmit

echo "✅ Pre-commit checks passed!"
